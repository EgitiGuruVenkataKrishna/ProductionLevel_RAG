# ==================================================
# Legal RAG — Production Dependencies
# ==================================================

# Core API
fastapi==0.109.0
pydantic>=2.0

# ASGI Server
uvicorn>=0.25.0
gunicorn>=21.2.0

# HTTP Client
httpx>=0.25.0

# LLM
groq>=0.4.0
langgraph>=0.0.26
langchain-core>=0.1.30

# Retrieval / Vector DB
faiss-cpu>=1.7.4
bm25s>=0.1.7
numpy>=1.24.0
nltk>=3.8.1  # For sentence boundary aware chunking fallback
fastembed>=0.2.6  # Local embedding & reranking (lightweight alternative to sentence-transformers)
aiosqlite>=0.20.0  # Async SQLite for chunk metadata
pinecone-client>=3.0.0
cohere>=5.0.0

# BM25 Keyword Search
bm25s>=0.2.0

# Rate Limiting
slowapi>=0.1.9

# Environment
python-dotenv>=1.0.0

# ==================================================
# LOCAL ONLY — For building indices (not deployed)
#   pip install sentence-transformers langchain-community pypdf
# ==================================================